<div class="container">
<h1 class="text-center mt-5"><%= t 'models.Users' %></h1>
 <a href="/" class="mt-5">&#8592; <%= t 'global.buttons.back to home'%></a>



<a href="/users/new">
<span style="font-size: 48px; color: Dodgerblue;" class="d-block mt-3">
<i class="fas fa-plus-circle"></i>
</span>
</a>

   <table class="table table-hover">
    <thead class="table-info">
      <tr>
       <th><%= t 'pages.Users.forms.id' %></th>
       <th><%= t 'pages.Users.forms.name' %></th>
       <th><%= t 'pages.Users.forms.email' %></th>
       <th><%= t 'pages.Users.forms.password' %></th>
       <th></th>
       <th></th>
       <th></th>
      </tr>
    </thead>
    <tbody>
     <% @users.each do |user| %>
      <tr>
        <td># <%=user.id%></td>
        <td><%=user.name%></td>
        <td><%=user.email%></td>
        <td>******</td>
         <td class="px-0"><%= link_to t('global.buttons.show'), user_path(user), class: "btn btn-success"  %></td>
         <td class="px-0"><%= link_to t('global.buttons.edit'), edit_user_path(user), class: "btn btn-primary"  %></td>
         <td class="px-0"><%= button_to t('global.buttons.delete'), user_path(user), method: :delete, class: "btn btn-danger"  %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
 

</div>


