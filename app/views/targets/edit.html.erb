<h1><%= t('pages.Targets.forms.editing target') %></h1>

<%= render "form", target: @target %>

<br>

<div>
  <%= link_to t('pages.Targets.buttons.show target'), @target %> |
 <%= link_to t('pages.Targets.buttons.back to targets'), targets_path %>
</div>
